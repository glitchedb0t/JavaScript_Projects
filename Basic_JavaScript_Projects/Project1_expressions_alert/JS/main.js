// Creating two variables - sent1 and sent2
var sent1 = ("This is the beginning of the string,"), sent2 = (" and this is the end of the string.")

// Concatenating sent1 and sent2 variables together
document.write(sent1 + sent2);

// Writing an expression
window.alert(10 / 5);

// Both document.write() and window.alert() used above.
// main.js is linked in the index.html file.